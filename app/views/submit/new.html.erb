<% if flash[:notice] %>
  <div id="flash" style="color: red">
    <%= flash[:notice] %>
  </div>
<% end %>

<div>
  <h1>依頼する内容</h1>
  <div>
    <%= render 'form' %>
  </div>
</div>
<%= link_to '戻る' , user_submit_index_path(@submit_request.user_id) %>
